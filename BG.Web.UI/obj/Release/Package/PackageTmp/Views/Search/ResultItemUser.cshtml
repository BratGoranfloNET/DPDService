@model UserViewModel

@{ 
	var item = Model;
}

<li>
	<p class="result-type">
		<span class="label label-danger">@SearchResources.UserItemLabel</span>
	</p>
	<a href="@Url.Action(nameof(PlatformController.UsersEdit), nameof(PlatformController).RemoveControllerSuffix(), new { @id = item.Id })" class="has-thumb">
		<div class="row">
			<div class="col-md-12">
				<div class="result-thumb pull-left">
					@if (item.ImageBlobId.HasValue)
					{
						<img class="img-thumbnail" src="@Url.Image(item.ImageBlobName, item.Name, 75, 75)" alt="@item.Name" />
					}
					else
					{
						<i class="fa fa-building"></i>
					}
				</div>
				<p class="h3 title text-primary">@item.Name</p>
				<p class="description">@item.Biography.WhenNullOrWhiteSpace(item.StatusMessage.WhenNullOrWhiteSpace(SearchResources.SearchEmptyDescriptionMessage)).</p>
			</div>
		</div>
	</a>
</li>
